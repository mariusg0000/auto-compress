# Session Decision Summary: README sync

Date: 2026-07-16 20:30
Base commit: caed7c8

## Context
User requested verification and update of README.md against the current codebase, recent decisions, and git history. The README had stale sections from before the V2 removal and from the 2026-06-12 docs sync.

## Changes Made
- Removed obsolete V2 Plugin section (V2 directory was deleted in caed7c8).
- Corrected runtime flow numbering and removed a transcript behavior claim inconsistent with current code.
- Added a new Summary Prompt Contract section matching the current `summarizePrunedMessages` prompt.
- Added `token-calc.log` to the State And Files table.
- Clarified summary-model fallback behavior in the Invariants section.
- Updated Last modified date to 2026-07-16.

## Decisions And Rationale
- V2 section removed because the V2 directory no longer exists.
- Transcript behavior corrected because the current code builds transcript text only from user/assistant text and reasoning parts, not from tool metadata.
- Prompt contract section added because the current prompt explicitly preserves Requirement/Plan/Tasks/Decision/Analysis result/Implemented/Validation/Status as structured facts.
- Model fallback clarified because code searches recent assistant messages first, then falls back to disk Build model, then to `deepseek-v4-flash`.

## Implementation Approach
- Compared README against `auto-compress.js`, recent decisions, and git log.
- Applied a scoped patch to README only.
- Validated with `node --check auto-compress.js` and `git diff --check`.

## Files Included
- README.md: synchronized documentation with current plugin behavior.
- decisions/2026-07-16-2030-readme-sync.md: records rationale for README updates.

## Commit Linkage
This summary is committed together with the README updates to keep rationale linked to documentation history.